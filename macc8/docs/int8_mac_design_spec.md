# MACC-8 int8 MAC — Design Spec

TODO: copy in the design spec deliverable (register map, protocols, latencies,
parameter values) referenced by `macc8_workspace_structure.md`. This is the
authoritative source `CLAUDE.md` reads before writing any RTL.
