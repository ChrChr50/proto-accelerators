# tool outputs
build/
pd/openlane/runs/
pdk/
verif/vectors/*
!verif/vectors/.gitkeep
sim_build/
obj_dir/
*.vcd
*.fst
*.log
!reports/**/*.log
__pycache__/
.venv/
# cocotb / SymbiYosys scratch runs (verif/formal/<task>/, from manually running
# run_formal.sh directly) -- the committed record lives under reports/, see
# reports/README.md and the Makefile's lint/sim*/formal targets.
verif/cocotb/results.xml
verif/formal/*/
# PDK / big binaries
*.gds
*.lef
*.def
!**/tech/**  # keep tech LEFs if vendored
